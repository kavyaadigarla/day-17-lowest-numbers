#include<stdio.h>
main()
{
	int n,c,i;
	printf("enter the value of n\n");
	scanf("%d",&n);
	while(i<=n)
	{
		if(n%i==0)
		{
			c++;
		}
			i++;
		}
		if(c==2)
		printf("prime");
		else
		printf("not a prime");
 }
